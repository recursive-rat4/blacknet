ktor {
   deployment {
        host = localhost
        port = 8285
        shutdown.url = "/api/shutdown"
   }

    application {
        modules = [ ninja.blacknet.api.APIServerKt.APIServer ]
    }
}
